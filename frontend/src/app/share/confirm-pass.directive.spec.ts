import { ConfirmPassDirective } from './confirm-pass.directive';

describe('ConfirmPassDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPassDirective();
    expect(directive).toBeTruthy();
  });
});
